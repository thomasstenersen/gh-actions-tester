name: Release
on:
  release:
    types: [created]


jobs:
  publish:
    environment:
      name: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Check release
        run: |
          echo ${{ github.event.release.assets }}
          echo ${{ github.event.tarball_url }}
