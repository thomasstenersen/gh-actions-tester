name: Release
on:
  release:
    types: [created]


jobs:
  publish:
    environment:
      name: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Check release
        run: |
          echo ${{ release.assets }}
          echo ${{ release.tarball_url }}
